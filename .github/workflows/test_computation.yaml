name: Run Bash computation tests before Merge

on: 
    pull_request:
        branches:
            - main

jobs:
    test:
        name: Run bash script tests
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
            - uses: actions/checkout@v4

            - name: Run bash all tests script
              run: ./run_tests.sh

